# Quote Generator

This is a React-based Quote Generator application.

## Features
- Generates random quotes every time
- Frontend built with React
- Backend built with Node.js & Express
- API-based quote fetching
- Ready for WhatsApp API integration